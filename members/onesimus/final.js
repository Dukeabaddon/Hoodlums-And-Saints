function abc(offset) {

    var resize = document.getElementById('blus')
    var prof = document.getElementById('blus2')

    if(resize.className == "blu") 
    {
        resize.className = "blu2"
        
    }
    else
    {
        resize.className = "blu"
    }

    
    isMoved = !isMoved
}